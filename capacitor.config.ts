import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gnotes.app',
  appName: 'G-Notes',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
